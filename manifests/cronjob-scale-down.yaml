apiVersion: batch/v1
kind: CronJob
metadata:
  name: mindbox-scale-down-pods
spec:
  schedule: "0 23 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: mindbox-service-account
          containers:
            - name: kubectl
              image: bitnami/kubectl
              command:
                - /bin/sh
                - -c
                - kubectl scale deployment mindbox-deployment --replicas=3
          restartPolicy: OnFailure